# Deployment-time bindings (dev). Do not put secret VALUES here.
# Use Secret Manager secret NAMES for SECRETS; values are resolved by Cloud Run.

# Attach Secret Manager secrets as environment variables
# Example maps secret name "api-token" to env var API_TOKEN using latest version
SECRETS="API_TOKEN=api-token:latest"

# Regular, non-secret environment variables for the service
ENV_VARS="ENV=dev,LOG_LEVEL=info"