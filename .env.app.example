# Application runtime configuration (local/dev only, non-secret)

# Environment name
ENV=dev

# Logging level: debug|info|warning|error|critical|notset
LOG_LEVEL=info

# Output logs as JSON (Cloud Run default) or plain text
LOG_JSON=true

# Local development port
PORT=8080

# Example feature flags (add as needed)
# FEATURE_X_ENABLED=true